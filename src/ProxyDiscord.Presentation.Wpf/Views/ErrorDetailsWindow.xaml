<Window x:Class="ProxyDiscord.Presentation.Wpf.Views.ErrorDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Erro completo" Width="720" Height="440" MinWidth="480" MinHeight="280"
        ResizeMode="CanResize" WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="ErrorTextBox" Grid.Row="0" IsReadOnly="True" AcceptsReturn="True"
                 TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Disabled" VerticalContentAlignment="Top"
                 Foreground="{StaticResource StatusErrorBrush}" Margin="0,0,0,12"/>
        <Button x:Name="CopyButton" Grid.Row="1" Content="Copiar" Click="CopyError_Click"
                HorizontalAlignment="Right" MinWidth="96"
                Style="{StaticResource PrimaryButtonStyle}"/>
    </Grid>
</Window>
